repos:
  - repo: local
    hooks:
      - id: shellcheck
        name: shellcheck
        entry: mise exec -- shellcheck -s bash --rcfile=.shellcheckrc
        language: system
        types: [shell]
        files: ^(bin/openspecx|\.mise/tasks/)

      - id: check-version-bump
        name: Check version bump in versioned files
        entry: mise run version:check
        language: system
        files: ^resources/(.*/SKILL\.md|claude/commands/opsx/.*\.md|opencode/commands/opsx-.*\.md|.*/scripts/openspec-auto|.*/agents/.*\.md)$
        pass_filenames: false
