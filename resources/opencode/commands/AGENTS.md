# Commands (OpenCode)

OpenCode slash commands, one file per command.

## Layout

Flat directory: `commands/osx-<command>.md`. No subdirectories.

## Categories

| Category | Files |
|----------|-------|
| Phase commands | `osx-phase0.md` through `osx-phase6.md` (one per phase) |
| Workflow | `osx-modify.md`, `osx-review.md`, `osx-verify-tests.md`, `osx-changelog.md`, `osx-maintain-docs.md` |

The full list lives in `manifest.toml` under `[resources.commands.*]`.

## Frontmatter

OpenCode commands use minimal frontmatter:

```yaml
---
description: <one-line purpose> # Required
---
```

## Phase Command Contract

`osx-phase0.md` … `osx-phase6.md` correspond 1:1 with the orchestrator's PHASE0–PHASE6. The orchestrator dispatches the matching command at the start of each phase. See `source/orchestrator/AGENTS.md` for the dispatch table.

## Conventions

- Filename `osx-<command>.md` must match the slash-command name `/osx-<command>`.
- Phase commands are the **only** entry points the orchestrator uses; do not rename or remove them without updating `PHASE_COMMANDS` in `source/orchestrator/engine.py`.
- Each phase command inherits the **Mandatory Start**, **Mandatory End**, and **blocker semantics** from `resources/opencode/skills/references/phase-protocol-common.md` and `blocker-semantics.md`. State the phase-specific differences inline; reach for the shared references for boilerplate.

## Claude Mirror

The Claude mirror at `resources/claude/commands/osx/` is generated by `mise run sync-mirrors`. Edit the OpenCode file first, regenerate, commit both. Token substitution handles the platform-specific bits (slash command form, ask-tool name, platform dir path).

The same script also emits a Claude **skill** mirror at `resources/claude/skills/osx-<X>/SKILL.md` for every opencode command. This dual-emit (legacy `.claude/commands/osx/<X>.md` + modern `.claude/skills/osx-<X>/SKILL.md`) mirrors upstream OpenSpec v1.7.0's own strategy on Claude Code. See `resources/claude/AGENTS.md` for the rationale.

## See Also

- `resources/AGENTS.md` — Manifest format
- `source/orchestrator/AGENTS.md` — Phase dispatch
- `resources/opencode/AGENTS.md` — Platform overview
- `resources/claude/AGENTS.md` — Dual-emit rationale
- `resources/opencode/skills/references/phase-protocol-common.md` — Mandatory Start / Mandatory End / state transitions
- `resources/opencode/skills/references/blocker-semantics.md` — When to halt vs continue
